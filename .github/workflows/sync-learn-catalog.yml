name: Sync Microsoft Learn catalog

on:
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fetch Microsoft Learn catalog
        run: node scripts/sync-learn-catalog.mjs
      - name: Commit changes
        run: |
          git config user.name "training-bot"
          git config user.email "training-bot@users.noreply.github.com"
          git add catalog/learn-modules.json
          git diff --cached --quiet || git commit -m "chore: sync Microsoft Learn catalog"
          git push
